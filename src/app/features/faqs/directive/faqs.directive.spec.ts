import { FaqsDirective } from './faqs.directive';

describe('FaqsDirective', () => {
  it('should create an instance', () => {
    const directive = new FaqsDirective();
    expect(directive).toBeTruthy();
  });
});
