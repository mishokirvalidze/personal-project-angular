import { BrandsDirective } from './brands.directive';

describe('BrandsDirective', () => {
  it('should create an instance', () => {
    const directive = new BrandsDirective();
    expect(directive).toBeTruthy();
  });
});
